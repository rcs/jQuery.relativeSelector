// Generated by CoffeeScript 1.2.1-pre
(function() {

  (function($) {
    return $.fn.relativeSelector = function() {
      var c, classes, tag, tag_and_class;
      tag = this.get(0).nodeName.toLowerCase();
      if (this.attr('id')) {
        return "#" + (this.attr('id'));
      } else if (['body', 'head', 'html'].some(function(test) {
        return test === tag;
      })) {
        return tag;
      } else {
        classes = "";
        if (this.attr('class')) {
          classes = ((function() {
            var _i, _len, _ref, _results;
            _ref = this.attr('class').split(' ');
            _results = [];
            for (_i = 0, _len = _ref.length; _i < _len; _i++) {
              c = _ref[_i];
              _results.push("." + c);
            }
            return _results;
          }).call(this)).join('');
        }
        tag_and_class = "" + tag + classes;
        if ($(tag_and_class).length === 1) {
          return tag_and_class;
        } else {
          return "" + (this.parent().relativeSelector()) + " " + tag_and_class + ":eq(" + (this.parent().children(tag_and_class).index(this)) + ")";
        }
      }
    };
  })(jQuery);

}).call(this);
